require 'test_helper'

class TwitterConfsController < ActionDispatch::IntegrationTest
  test 'it writes the expected file' do
    assert false
  end

  test 'it sends the correct variables to the template' do
    assert false
  end
end

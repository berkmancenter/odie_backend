require 'test_helper'

class TwitterConfTest < ActiveSupport::TestCase
  test 'valid? is in sync with REQUIRED_VARS' do
    assert false
  end

  test 'generate returns a template result' do
    assert false
  end
end
